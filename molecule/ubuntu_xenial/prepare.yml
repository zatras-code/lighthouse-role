---
- name: Prepare
  hosts: all
  become: true

  tasks:
    - name: Install packages on OracleLinux
      ansible.builtin.shell: |
        dnf install -y git nginx --setopt=timeout=300
      when: ansible_facts['os_family'] == "RedHat"
